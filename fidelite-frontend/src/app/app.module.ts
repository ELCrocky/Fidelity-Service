import { NgModule } from '@angular/core';

// App now bootstraps standalone via bootstrapApplication() in main.ts.
// Providers and routing are configured in app.config.ts.
@NgModule({})
export class AppModule {}