server:
  port: 8080

spring:
  profiles:
    active: dev   # remove for production
  datasource:
    url: jdbc:postgresql://localhost:5432/fidelite_db
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 007omerbond 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: 86400000  # 24 hours

cors:
  allowed-origins: http://localhost:4200
